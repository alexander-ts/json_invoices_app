name: json_invoices_app
description: "App that shows invoices from local JSON file."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: 4.8.1
  json_serializable: 6.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.9
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
